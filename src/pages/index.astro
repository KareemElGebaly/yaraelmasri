---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('posts');
---

<Layout title="مدونة يارا" description="ترجمات أدبية من الصينية إلى العربية ونصائح للمترجمين.">
  <h2 style="text-align:center; font-size: 1.5rem; margin-bottom: 2rem;">أحدث المنشورات</h2>

  <ul style="list-style: none; padding: 0;">
    {posts.map(post => (
      <li style="margin-bottom: 2rem; border-bottom: 1px solid #eee; padding-bottom: 1.5rem;">
        <a href={`/posts/${post.slug}/`}>
          <h3 style="margin: 0 0 0.5rem;">{post.data.title}</h3>
        </a>
        <p style="color: #777; margin: 0 0 0.5rem;">📅 {new Date(post.data.pubDate).toLocaleDateString('ar-EG')}</p>
        <p style="margin: 0 0 0.5rem;">{post.data.description}</p>
      </li>
    ))}
  </ul>
</Layout>